apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: compute-claim1
  name: compute-claim1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
