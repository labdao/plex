apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: compute-claim0
  name: compute-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
