# The openlab client
The openlab client is the first client developed for the lab-exchange. The client is based on the [open CLI framework](https://oclif.io/) and written in typescript.

**currently the client is in alpha stage only, expect dragons.**

## contributing to the openlab client
To install the latest branch of the command line interface, clone the GitHub repository from the latest branch

```
git clone https://github.com/labdao/openlab-cli
# within the repository switch to the latest branch
git checkout -b cli-subgraph-a
```

Install dependencies with ``` npm i ```. 






